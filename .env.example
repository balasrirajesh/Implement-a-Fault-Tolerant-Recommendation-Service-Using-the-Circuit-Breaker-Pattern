# ─── Recommendation Service Configuration ────────────────────────────────────
# Copy this file to .env and adjust values as needed.
# Usage: cp .env.example .env && docker-compose up -d

# Port on which the recommendation-service listens (exposed to host)
API_PORT=8080

# Internal Docker network URLs for dependency services
# (Used by recommendation-service to reach its dependencies)
USER_PROFILE_URL=http://user-profile-service:8081
CONTENT_URL=http://content-service:8082
TRENDING_URL=http://trending-service:8083

# Individual service ports (used inside containers)
USER_PROFILE_PORT=8081
CONTENT_PORT=8082
TRENDING_PORT=8083

# ─── Circuit Breaker Configuration (for reference) ───────────────────────────
# These are hardcoded in the service but documented here for visibility.
# CB_REQUEST_TIMEOUT_MS=2000
# CB_WINDOW_SIZE=10
# CB_FAILURE_RATE_THRESHOLD=0.5
# CB_CONSECUTIVE_FAILURE_THRESHOLD=5
# CB_OPEN_STATE_DURATION_MS=30000
# CB_HALF_OPEN_MAX_TRIALS=3